{
  "name": "survey",
  "version": "1.0.0",
  "description": "Survey app with DWG viewer",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jsdom": "^22.1.0"
  },
  "dependencies": {
    "playwright": "^1.58.0",
    "puppeteer-core": "^21.11.0"
  }
}
