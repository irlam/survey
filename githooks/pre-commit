#!/bin/sh
# Simple pre-commit hook to run validation
php tools/validate_tools.php
rc=$?
if [ $rc -ne 0 ]; then
  echo "Pre-commit validation failed. Fix issues before committing."
  exit $rc
fi
exit 0
